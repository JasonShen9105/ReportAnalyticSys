<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ap.kl.mapper.UserRoleMapper">

	<!-- 通用查询结果列 -->
	<sql id="Base_Column_List">
		id, uid, rid
	</sql>
    <select id="selectMaxID" resultType="UserRole">
		SELECT MAX(id) as id FROM user_role 
	</select>
</mapper>